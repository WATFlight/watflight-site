# 问题修复记录

## 已修复内容

1. 调整首屏导航栏与图片区域的位置关系，为导航栏和左右图片预留独立的垂直空间，避免相互重叠，并适当增大两者间距。
2. 调整首屏左右图片的垂直偏移，使图片完整显示在导航栏下方。
3. 在首屏图片动画完全结束后增加 `35vh` 的固定展示距离，用户继续下滑后再进入下一部分。
4. 将 “The sky was never the limit.” 与 “Where We Compete” 统一到相同宽度的内容容器，使两者左边界对齐。
5. 移除 “Partner With Us” 区域重复的顶部留白，缩短上一部分分界线与该标题之间的距离。
6. 将手机端社交链接调整为两行两列的等宽按钮网格，统一按钮尺寸和间距，扩大触控区域，并缩短社交区域上下方的多余留白；平板和桌面端使用一行四列的紧凑布局。
7. 修复刷新页面时导航栏背景闪烁的问题；导航栏及移动菜单现在始终使用深色磨砂背景，不再根据页面滚动位置切换透明度和颜色。
8. 移除桌面端导航栏 “Join” 按钮的外边框，并将默认文字颜色统一为与其他导航项相同的浅灰色，保留原有间距和悬停反馈。
9. 重新整理赞助等级卡片顶部布局，将等级徽标移动到标题右侧并放大到统一尺寸；星号改用清晰的星形图标，`01/02` 使用等宽数字显示，同时减少标题区域的多余空白。
10. 将 “Get Involved” 区域限制为与其他主要内容一致的居中最大宽度，统一分割线、标题和三列步骤的左右边界。
11. 移除 Team 区域重复的顶部留白，缩短赞助联系文案与 Waterloo Engineering 标志之间的距离，同时保留 Sponsorship 区域原有的底部间距。
12. 将上一部分分界线到 “Partner With Us” 的距离统一为 `80px`，使其与分界线到 “Get Involved” 的距离一致，不再随桌面断点额外增大。
13. 统一 “Competing on the world stage.”、“Help us build the future of flight.” 和 “Want to join WATFlight?” 三个同级标题的文本宽度、字号阶梯、字重与字距，并移除强制换行，使其按相同规则自然排版。
14. 完成代码库六阶段优化：恢复质量门禁、清理模板遗留代码与资源、收窄客户端组件边界、恢复图片优化、合并滚动监听并降低重渲染、集中站点内容与共享标题样式。
